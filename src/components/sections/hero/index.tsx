import styles from './styles.module.scss';

const Hero = () => {
  return (
    <section className={styles.hero}>
      <h1 className={styles.hero__title}>crocode</h1>
    </section>
  );
};

export default Hero;
